name: rust ci

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: checkout speer
        uses: actions/checkout@v4
        with:
          path: speer

      - name: checkout speer-sys
        uses: actions/checkout@v4
        with:
          repository: jk20342/speer-sys-rust
          path: speer-sys

      - name: setup rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: install native deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake clang libclang-dev pkg-config

      - name: fmt
        working-directory: speer
        run: cargo fmt --check

      - name: check
        working-directory: speer
        run: cargo check --features full-chat

      - name: clippy
        working-directory: speer
        run: cargo clippy --features full-chat -- -D warnings

      - name: doc (rustdoc)
        working-directory: speer
        env:
          RUSTDOCFLAGS: -Dwarnings
        run: cargo doc --no-deps --features full-chat
